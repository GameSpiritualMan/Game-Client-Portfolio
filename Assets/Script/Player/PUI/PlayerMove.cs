using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMove : MonoBehaviour
{
    //playercontroller의 정보를 가져오기 위해 선언한다.
    private PlayerController playerController;
    void Start()
    {
        
    }

    ////왼쪽이동
    //public void LeftMove()
    //{
    //    //Player태그를 가진 오브젝트의 PlayerController를 재정의 한다.
    //    playerController = GameObject.FindWithTag("Player").GetComponent<PlayerController>();
    //    //PlayerController스크립트의 LeftMove함수를 실행한다.
    //    playerController.LeftMove();
    //}

    //public void RightMove()
    //{
    //    //Player태그를 가진 오브젝트의 PlayerController를 재정의 한다.
    //    playerController = GameObject.FindWithTag("Player").GetComponent<PlayerController>();
    //    //PlayerController스크립트의 RightMove함수를 실행한다.
    //    playerController.RightMove();
    //}

    //public void UpMove()
    //{
    //    //Player태그를 가진 오브젝트의 PlayerController를 재정의 한다.
    //    playerController = GameObject.FindWithTag("Player").GetComponent<PlayerController>();
    //    //PlayerController스크립트의 UpMove함수를 실행한다.
    //    playerController.UpMove();
    //}

    //public void DownMove()
    //{
    //    //Player태그를 가진 오브젝트의 PlayerController를 재정의 한다.
    //    playerController = GameObject.FindWithTag("Player").GetComponent<PlayerController>();
    //    //PlayerController스크립트의 DownMove함수를 실행한다.
    //    playerController.DownMove();
    //}

    //public void StopMove()
    //{
    //    //Player태그를 가진 오브젝트의 PlayerController를 재정의 한다.
    //    playerController = GameObject.FindWithTag("Player").GetComponent<PlayerController>();
    //    //PlayerController스크립트의 StopMove함수를 실행한다.
    //    playerController.StopMove();
    //}
}
